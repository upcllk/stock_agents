# 数据库
psycopg2-binary>=2.9.9

# 定时任务
APScheduler>=3.10.0

# 配置
python-dotenv>=1.0.0

# LLM 调用（按实际使用的 API 二选一或都装）
openai>=1.0.0
# httpx>=0.27.0

# Pydantic（数据验证）
pydantic>=2.0.0

# LangGraph（搜索→解析图）
langgraph>=0.2.0
langchain-openai>=0.2.0
